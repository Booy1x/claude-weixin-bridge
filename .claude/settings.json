{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"When fetching web content, default to mcp__modelcontextprotocol-servers-fetch__fetch. Use WebFetch only as fallback when MCP fetch is unavailable or fails, and explicitly tell the user before fallback.\"}}'"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"WebFetch is fallback-only: use it after MCP fetch is unavailable/failed and notify the user before fallback.\"}}'"
          }
        ]
      }
    ]
  }
}
